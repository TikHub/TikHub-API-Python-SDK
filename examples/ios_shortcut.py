"""Example: iOS-Shortcut

SDK attribute: ``client.ios_shortcut``
Endpoints: 1

Usage::

    python examples/ios_shortcut.py

AUTO-GENERATED by scripts/generate_examples.py.
"""

import asyncio
import json

from tikhub import AsyncTikHub

API_KEY = "YOUR_API_KEY"


async def main():
    async with AsyncTikHub(api_key=API_KEY) as client:

        # GET /api/v1/ios_shortcut/shortcut
        # 用于iOS快捷指令的版本更新信息/Version update information for iOS shortcuts
        result = await client.ios_shortcut.shortcut()
        print(json.dumps(result, indent=2, ensure_ascii=False))


if __name__ == "__main__":
    asyncio.run(main())
