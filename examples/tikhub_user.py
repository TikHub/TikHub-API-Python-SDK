"""Example: TikHub-User-API

SDK attribute: ``client.tikhub_user``
Endpoints: 6

Usage::

    python examples/tikhub_user.py

AUTO-GENERATED by scripts/generate_examples.py.
"""

import asyncio
import json

from tikhub import AsyncTikHub

API_KEY = "YOUR_API_KEY"


async def main():
    async with AsyncTikHub(api_key=API_KEY) as client:

        # GET /api/v1/tikhub/user/get_user_info
        # 获取TikHub用户信息/Get TikHub user info
        result = await client.tikhub_user.get_user_info()
        print(json.dumps(result, indent=2, ensure_ascii=False))

        # GET /api/v1/tikhub/user/get_user_daily_usage
        # 获取用户每日使用情况/Get user daily usage
        result = await client.tikhub_user.get_user_daily_usage()
        print(json.dumps(result, indent=2, ensure_ascii=False))

        # GET /api/v1/tikhub/user/calculate_price
        # 计算价格/Calculate price
        result = await client.tikhub_user.calculate_price(endpoint='/api/v1/douyin/app/v1/fetch_one_video', request_per_day=100000)
        print(json.dumps(result, indent=2, ensure_ascii=False))

        # GET /api/v1/tikhub/user/get_tiered_discount_info
        # 获取阶梯式折扣百分比信息/Get tiered discount percentage information
        result = await client.tikhub_user.get_tiered_discount_info()
        print(json.dumps(result, indent=2, ensure_ascii=False))

        # GET /api/v1/tikhub/user/get_endpoint_info
        # 获取一个端点的信息/Get information of an endpoint
        result = await client.tikhub_user.get_endpoint_info(endpoint='/api/v1/douyin/app/v1/fetch_one_video')
        print(json.dumps(result, indent=2, ensure_ascii=False))

        # GET /api/v1/tikhub/user/get_all_endpoints_info
        # 获取所有端点信息/Get all endpoints information
        result = await client.tikhub_user.get_all_endpoints_info()
        print(json.dumps(result, indent=2, ensure_ascii=False))


if __name__ == "__main__":
    asyncio.run(main())
